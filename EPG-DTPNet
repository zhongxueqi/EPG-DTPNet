# -*- coding: utf-8 -*-
"""
EPG-DTPNet: Extreme-value Prior-Guided Dual-Branch Deep Temporal Prediction Network

The complete code (Total lines of code: 952) will be made publicly available upon acceptance of the paper.

=====================================================================================

Core contributions:
1. Variational Mode Decomposition (VMD) based signal preprocessing
2. Dual-branch architecture to decouple waveform shape and response amplitude
3. Physically constrained composite loss function with multi-dimensional regularization
4. End-to-end training, reconstruction and evaluation pipeline

Reference:
    Paper under review in Engineering Structures
    The complete code (Total lines of code: 952) will be made publicly available upon acceptance of the paper.
"""

import os
import time
import datetime
import random
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import joblib

# Data preprocessing and metrics
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import mean_squared_error, mean_absolute_error

# Deep learning framework
import tensorflow as tf
from tensorflow.keras.models import Model
from tensorflow.keras.layers import (
    Input, Conv1D, LSTM, Bidirectional, Dense, RepeatVector, TimeDistributed,
    Lambda, GaussianNoise, Concatenate, Dropout, SpatialDropout1D,
    Attention, Add, LayerNormalization, MultiHeadAttention
)
from tensorflow.keras.optimizers import Adam
from tensorflow.keras.callbacks import EarlyStopping

# Signal decomposition
from vmdpy import VMD

import warnings
warnings.filterwarnings('ignore')
os.environ['TF_CPP_MIN_LOG_LEVEL'] = '2'  # Suppress redundant TF logs



